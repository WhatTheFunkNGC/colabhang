F

Format of Shared variables

lists consist of a base name (e.g. listA)
The variable named "listA" will contain the length of the list
The rest of the list will start from 1 to n (e.g listA1, listA2..... listAn)

In the lister function Lists named ListTxt to track the values of list entries